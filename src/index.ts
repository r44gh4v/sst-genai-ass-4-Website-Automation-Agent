import { startServer } from "./server";

/**
 * Entry point. Boots the local web server that hosts the prompt UI and drives
 * the browser automation agent. Open the printed URL, type a request naming any
 * website and action, and watch the agent perform it in a real browser window.
 */
startServer();
